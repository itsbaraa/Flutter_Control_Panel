import 'package:flutter/material.dart';

class UsbSerialControlScreen extends StatelessWidget {
  const UsbSerialControlScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text("USB Serial Control")), body: const Center(child: Text("USB Serial UI to be implemented.")));
  }
}