// lib/screens/bluetooth_control_screen.dart
import 'package:flutter/material.dart';

class BluetoothControlScreen extends StatelessWidget {
  const BluetoothControlScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Bluetooth Control")),
      body: const Center(child: Text("Bluetooth UI to be implemented.")),
    );
  }
}
